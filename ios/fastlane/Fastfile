default_platform(:ios)

platform :ios do
  desc "Build for simulator"
  lane :build do
    xcodebuild(
      workspace: "RNFLIOSDemo.xcworkspace",
      scheme: "RNFLIOSDemo",
      sdk: "iphonesimulator",
      configuration: "Debug",
      derivedDataPath: "../build",
      xcargs: "CODE_SIGN_IDENTITY='' CODE_SIGNING_ALLOWED=NO"
    )
  end
end
